import numpy as np

from src.utils import sign, PARAM_CONSTANTS, FLAGS, ITERATIONS, transform_params
from src.model import Model


class AutoCorrelationModel(Model):
    def __init__(self):
        pass
